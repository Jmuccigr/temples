// Store the database in a local variable so we don't
// need to manipulate it every time we do a query.

function checkRun (data) {
        if (newRun) {
        	allPoints = data.features;
        	newRun = false;
        } else {
        }
}
